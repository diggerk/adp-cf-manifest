---
deployment:
  name: "adp"
  domain: "<%= node[:cloudfoundry][:domain] %>"
jobs:
  install:
    - router
  installed:
    - nats_server:
      host: "<%= node[:cloudfoundry][:nats_ip] %>"
      port: "4222"
      user: "nats"
      password: "nats"
